name: Checker

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  build:
    name: Check TWA
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Check if build directory exists
      id: check_build_dir
      run: test -d android/build && echo "Build directory found" || echo "Build directory not found"

    - name: Adjust gradlew permissions
      run: |
        ls -l android/gradlew
        chmod u+x android/gradlew
        ls -l android/gradlew
      if: steps.check_build_dir.outputs.status != 'Build directory found'

    - name: Run lint
      run: android/.gradlew lint || exit 1
      working-directory: android

    - name: Run tests
      run: android/.gradlew test || exit 1
      working-directory: android

    - name: Run TWA validation script
      run: android/.gradlew connectedAndroidTest || exit 1
      working-directory: android

    - name: Cleanup
      run: |
        rm -rf android/.gradle
      working-directory: android
